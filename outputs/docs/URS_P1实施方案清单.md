# URS 新需求 · P1 批次实施方案（待确认）

> 对照对象：`reference/URS.md` 的 FR-05（趋势/变化点）、FR-06（分级报警防抖）、FR-07（界面）
> 前置：P0 已上线（commit 810a293），见 `URS新需求覆盖对照清单.md`「五、P0 批次实施结果」

---

## 〇、设计口径（2026-09-09 用户拍板：**严格按 URS 原文设计**）

> **重要更新**：用户明确指示——P1 按 URS 需求原文口径实现，**不受当前库内稀疏数据束缚**；
> 测试用**自造的、与 `measure_param` 库读结构完全一致的连续分钟数据**来验证逻辑正确性；
> 待真实设备接入并产生连续数据后再做真实验证。

据此，P1 引擎默认以 URS 假定的 **连续 1-min 通气序列** 为输入（`X(t)`、`t=分钟`）：
- **防抖「持续 ≥60/30/15 min」= 真·连续分钟判定**（G1≥60 / G2≥30 / G3≥15）；回落 < 阈值并维持 ≥10min 自动降级解除。
- **CUSUM** 在逐分钟序列上递推 $S^\pm(t)=\max(0,S^\pm(t-1)\pm(X(t)-\mu_0-k))$；遇 >4h 断流重置状态。
- **斜率** 用 1h/6h/24h 三个滑动窗口，窗内点数 < N_min(默认3) 该档返回「数据不足」。
- **G0-G3 总评**按 FR-06 矩阵，由 24h TAT / 持续分钟 / 斜率方向 / 顺应性分层综合判定。

> 当前库内稀疏（23 通气分钟/24h）意味着这些引擎对真实数据暂不触发，
> 属**预期**；逻辑正确性由生成器测试保证，真实验证延后到设备接入连续数据。

---

## 一、本次 P1 范围（建议）

### P1-A：趋势分析 —— 斜率 + CUSUM 变化点（URS FR-05）
**后端**（`calculator.py` + `api.py`）
1. 新增 `linear_slope(points_xy)` → 线性回归斜率 β（最小二乘）。
2. 新增 `window_slopes(rows, key, wins_h)`：对 ΔP/MP 分别算 6h / 24h（1h 因点数不足通常弃用）
   的**末尾滑动时间窗**斜率 β（cmH2O/h、J/min/h）；窗内有效点 < N_min(默认 3) → 返回 None + `insufficient`。
3. 新增 `cusum_change_points(rows, key, mu0_span_h, k, h, gap_reset_min)`：
   - 基线 μ0 = 过去 24h 有效均值；容差 k、控制限 h（URS 建议 ΔP:k=2,h=?、MP:k=2；h 默认 =5σ）。
   - 遇 >4h 断流重置状态（不累积到新段）。
   - 返回 `{points:[{ts, side, S}], baseline_mu0, k, h}`。
4. `/api/trends`（新端点）或扩展现有 `/api/dp/trend`+`/api/mp/trend`：每个返回点附 `slope` 与
   变化点标记，前端不再用 `diff>3` 启发式。

**前端**
- `fetchTrends` 改用后端返回的 CUSUM 变化点（替换现有 `|diff|>3` 循环）。
- 每个 ΔP/MP 页顶部/统计区显示「当前 6h/24h 斜率 β」+ 方向箭头 + 是否显著。

### P1-B：G0-G3 风险矩阵 + 防抖动（URS FR-06）
**风险矩阵重定义**（关键决策，需您拍板——见"四、待拍板"）
- 复用现有 4 级刻度（L1≈G0 / L2≈G1 / L3≈G2 / L4≈G3），但把**顶部总评**改为按 URS 的
  「24h 窗口 TAT」判据驱动：
  - G0: ΔP TAT<5h 且 MP TAT<7h（且瞬时未越限）
  - G1: ΔP≥15 持续≥60min（或 5≤TAT<15）/ MP≥17 持续≥60min（或 7≤TAT<17）
  - G2: ΔP TAT≥15 或 [ΔP≥15 持续≥30min 且 6h 斜率>0] / MP TAT≥17
  - G3: ΔP TAT≥30 或 ΔP≥20 持续≥15min / MP 按顺应性（高:≥18×2h，低:≥20×12h）
- 新增**防抖状态追踪**：对"持续超标"需维护「超标起始/累计时长」状态（落到 metrics_1min 或
  独立状态集合），时长用**有效通气分钟**口径；回落 <阈值 且维持 ≥10min → 自动解除。

**预警**
- `_check_and_alert` 去重从「5min 内同(级别,类别)」改为「G 等级变更才新报 + 防抖达标才报」，
  并支持**自动解除**（写入 resolved 状态，前端显示"已恢复"）。

### P1-C：界面（URS FR-07）
1. **顺应性分层标签卡**（原 P2 #13 提前）：总览新增「CRS 数值 + 高/低顺应性 + 特异性安全功率上限」
   标签卡（数据已具备：`cumulative.compliance_mean/stratum`）。
2. **趋势图横轴按真实时间**（废弃 `X(idx)=idx/(win-1)` 等距），用 `ts` 映射时间轴。
3. **补 72h 档**：`/api/{dp,mp}/trend` 后端 hours 已支持 0.1~168h；前端 time-window 按钮加
   「72小时」，并让「24h 默认视图」数据加载更符合所选档。

### 明确**不做**（本轮）
- 波形积分 MP（用户已定优先固定公式）
- 32 床并发、断网缓存（用户已定不属范围）
- 交接班/班次导出（仍属 P2）
- 异常值过滤 + 免责声明常驻（属 P2）

---

## 二、需要触碰的文件

| 文件 | 改动 |
| :--- | :--- |
| `lung_protection_cockpit/calculator.py` | 新增 `linear_slope` / `window_slopes` / `cusum_change_points`；G0-G3 判级函数 |
| `lung_protection_cockpit/aggregator.py` | 防抖状态追踪、G 级判定入库、预警自动解除、斜率/CUSUM 写入分钟文档 |
| `lung_protection_cockpit/api.py` | 趋势端点带 slope/变化点；overview 顶部总评改 G0-G3；补顺应性卡数据 |
| `lung_protection_cockpit/config.py` | G0-G3 判据阈值、CUSUM k/h、防抖时长常量 |
| `cockpit_frontend.html` | 前端 CUSUM 用后端、趋势真实时间轴、72h 档、顺应性分层标签卡 |
| `outputs/docs/*` + `README.md` | 进度更新 |
| `outputs/scripts/*` | 复用 `rebuild_1min.py` 回填 |

---

## 三、验证方式（生成数据优先，真实验证延后）
- **生成器**：`scripts/gen_urs_testdata.py` 产出与 `measure_param` 读结构一致的连续分钟批次
  （每批次 16 参数、可指定起始/时长/呼吸力学轨迹），可直接喂 collector 全链路。
- **引擎单测**：`linear_slope`（已知斜率直线应还原 β）；`cusum`（造平移信号应检出、造 >4h
  断流应重置）；`DebounceGradeEngine`（ΔP≥15 持续 70min → G1 于第 60min 触发；回落 10min 自动解除）。
- 生成连续数据端到端：起后端 → 喂生成数据 → `/api/trends` 返回 slope + 变化点；G0-G3 总评与矩阵手算一致。
- 真实数据接入后：回填 `metrics_1min` 复测同一批断言，仅替换数据源。

---

## 四、口径落地（已由「严格按 URS」指令确定，实施中以 URS 为准）

1. **G0-G3 总评**：按 FR-06 矩阵原文，由 24h 滚动窗口 TAT + 持续分钟 + 斜率方向 + 顺应性分层判定；
   不再沿用「瞬时×累积 max」的旧口径（该口径是为迁就稀疏数据的妥协，已废弃）。
2. **CUSUM**：$k$ 按 URS（ΔP:2 cmH₂O / MP:2 J/min）；$h$（控制限）取 5σ，σ 用窗口内序列样本标准差估计。
3. **斜率 N_min** = 3，窗内有效点 <3 → 返回「数据不足」而非 0。
4. **72h 档**：照常提供 UI 档位；测试/后续数据不足时显示「该时间窗仅覆盖 Xh」空态，不隐藏档位。
5. **防抖「持续」**：真·连续分钟判定（见〇节），用生成器造连续场景验证阈值触发/解除。

---

请确认以上范围与 5 个口径后，我再开始 P1-A→C 的实现。
